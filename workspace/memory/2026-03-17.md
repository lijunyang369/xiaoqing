# 2026-03-17

- 用户确认：1-5（身份设定、称呼偏好、协作模式、固定 agent 分工、硬边界）应沉淀到长期记忆而非仅会话摘要。
- 用户授权小青负责记忆管理，并明确：不确定信息应归档到长期记忆/session/thread哪一层时，可先询问用户。
- 已创建并初始化 `MEMORY.md`，完成上述长期规则沉淀。
- 用户同意《记忆分层标准 v1（10条）》及默认执行策略，已写入长期记忆。
- 用户同意接入 Self-Improving 技能，已写入《Self-Improving 接入策略 v1（无冲突）》：明确 MEMORY.md 为长期唯一权威，self-improving 仅作为学习流水线与候选提炼层。
- 用户对本次排障流程提出纠正：应优先参考官方社区高频解法；已记录为自我学习条目并调整后续默认排障顺序。
- 用户补充规则：此类纠错应由小青自动触发自我学习，不需要用户额外提示。
- 本机排障关键结论：`winget` 异常核心由 TLS 配置导致；检查发现仅启用到 TLS1.1，开启 TLS1.2 后 `winget source update` 恢复正常。
- 终端环境偏好：用户明确希望 Windows 终端尽量对齐 macOS/zsh 习惯（快捷键、界面与字体风格）。
- 终端安装冲突经验：Scoop 版 `windows-terminal` 与系统版“终端”可能产生启动链路冲突；后续优先保留单一来源安装，避免并存。
- 已执行记忆规整：将“排障顺序（官方优先）/winget-TLS1.2 优先检查/Terminal 单一来源安装”提炼进长期记忆。
- 用户纠正：`winget` TLS1.2 与 Terminal 单一来源属于一次性任务结论，不应写入长期记忆；已从 `MEMORY.md` 回滚，仅保留长期可复用的流程级规则。
- 用户新增长期要求：小青增加“TOC（制约理论）专家”身份，后续决策默认围绕聚焦五步骤进行。
