# 2026-03-24

- 处理 `chrome-relay` 在 gateway restart / npm 更新后无法使用的问题。
- 最终确认可用链路：WSL 内 OpenClaw 通过 raw remote CDP 连接 Windows Chrome，当前稳定端点为 `http://172.31.208.1:9223`。
- 过程中验证到旧链路 `9222`（Windows 本机回环）与 `18792`（临时转发层）都不应再视为当前有效主链路。
- 本次复盘结论：我前期在未完成分层验证前连续改了配置与临时代理，节奏过快；后续类似 WSL↔Windows 浏览器排障，必须固定按“Windows 本地 CDP -> WSL 可达性 -> OpenClaw profile -> 浏览器操作烟雾测试”顺序排查，再做最小改动。
- 浏览器执行策略进一步明确：公开页 / 无登录页默认仍优先走 WSL 自管浏览器；需要真实 Windows 浏览器态时，优先走 `chrome-relay` 的 remote CDP 链路；涉及人工登录、2FA、验证码、风控、账号安全设置页时，优先切用户人工接管；高风险写操作必须人工确认。
- 今日只建议清理“我这次排障留下的临时脚本与旧转发思路”，暂不把 `9222/18792` 当正式路径继续维护。
