# 2026-03-23

- 用户明确要求记住：其电脑是 Windows 11 系统，OpenClaw 安装在 WSL 内。
- 后续本机相关迁移、配置、排障默认按“Windows 11 宿主机 + WSL 内 OpenClaw”处理。
- 用户确认固化浏览器双轨策略：公开页/无登录页优先走 WSL 内 OpenClaw 自管浏览器；涉及人工登录、2FA、验证码、风控、账号安全设置页时优先切到用户真实浏览器；高风险写操作必须人工确认。
- 当前浏览器建设顺序已确定：先打通 WSL 内自管浏览器能力，再打通 Windows 用户浏览器接入链路。
- 今日完成一次浏览器能力打通闭环：1）启用 ACPX 与 Discord ACP thread spawn；2）确认 WSL 侧无本地浏览器后安装 Chromium；3）发现 WSL 无图形显示，切换 `browser.headless=true` 后成功启动 OpenClaw 自管浏览器并通过 GitHub 首页最小自检；4）为 Windows 真实浏览器补齐 `user` 与 `chrome-relay` 配置并安装扩展；5）通过 `chrome-relay` 成功接管 Windows Chrome 已附着标签页，验证 GitHub 登录态有效，且 `settings/keys`、`settings/tokens`、`settings/applications` 三页均可正常打开。
- 本次排障结论：在“Windows 11 宿主机 + WSL 内 OpenClaw”结构下，Windows Chrome 原始 remote-debugging 端口直连 WSL 不稳定，不应作为首选主路径；更稳的方案是双轨：公开页优先 WSL 内 OpenClaw 自管浏览器（headless），登录态/安全设置页优先 `chrome-relay` 或用户真实浏览器接管。
- 可复用执行顺序：先修通 ACP/runtime 配置，再打通 WSL 自管浏览器（必要时 headless），最后再接入 Windows 真实浏览器；这样可以先获得公开页能力，再补登录态能力，整体风险与耦合更低。
