## 2026-04-08 11:58 (Asia/Shanghai)

### 模型配置与会话清理任务

用户要求：
1. 全局默认模型改为 DeepSeek-chat adaptive
2. architect 角色策略改为 gpt-5.4 high
3. 清空所有会话

已完成：
- 修正了 `/home/lijunyang/.openclaw/openclaw.json` 中的无效配置：
  - 将 `agents.defaults.model.primary` 从 `openai-codex/gpt-5.4` 改为 `deepseek/deepseek-chat`
  - 删除了无效的 `agents.defaults.thinking` 键（OpenClaw 配置不支持此键）
- 在 `config/agent-role-registry.json` 中为 architect 角色添加了模型覆盖：
  ```json
  "architect": {
    "sessionType": "subagent",
    "protocol": "memory/long-term/BEHAVIOR-PROTOCOL-ARCHITECT.md",
    "labelHint": "architect - <task>",
    "spawnTemplate": "architect",
    "modelOverride": "openai-codex/gpt-5.4",
    "thinkingOverride": "high"
  }
  ```
- 在 `docs/spawn-task-templates.md` 的 architect 模板中明确指定了模型和推理级别：
  ```text
  你是 architect 角色，使用 openai-codex/gpt-5.4 模型和 high 推理级别。
  ```
- 更新了 `AGENTS.md` 的 "Agent Role Maintenance" 部分，说明 architect 角色使用特殊模型策略

待执行：
- 停止 OpenClaw gateway 服务
- 清空 `~/.openclaw/agents/*/sessions/` 目录下的所有会话存档文件
- 重启 gateway 使新配置生效

关键决策：
- 采用分层策略：全局默认用 DeepSeek-chat adaptive，architect 角色显式覆盖为 gpt-5.4 high
- 通过角色注册表和 spawn 模板实现模型覆盖，而非全局配置
- 使用 `gio trash` 而非 `rm` 清理会话文件，确保可恢复性
- 清理后重启 gateway 确保新会话按新配置启动

技术细节：
- OpenClaw 配置中的 `agents.defaults.thinking` 键无效，已删除
- 模型覆盖通过 `sessions_spawn` 的 `model` 和 `thinking` 参数实现
- 会话清理需要停止 gateway 以避免文件锁冲突

---

## 2026-04-08 16:55 (Asia/Shanghai)

### 建立 TOC 协作系统 v1

背景：
用户（俊阳）明确协作系统目标应为：通过“小青 + 俊阳 + 子进程”的协作，高质量按时完成尽量多的任务；要求用 TOC 思维构建一个持续优化系统。

关键纠偏：
- 先定义系统目标，再识别当前制约
- 要站在全局看问题，单个任务可交给子进程
- 优先给出最佳实践方案和关键决策点，再考虑其他可行方案
- TOC 不是泛化优先级，而是围绕当前制约最大化有效产出

已完成：
- 并行分发 architect 和 plan 两个子进程，分别产出系统蓝图与运行节奏
- 落地创建 `docs/TOC-COLLABORATION-SYSTEM.md`
- 落地创建 `docs/TOC-TEMPLATES.md`
- 创建 `TASK-PROGRESS.md` 作为最小控制面

当前系统假设：
- 当前主制约优先假设为“主链路决策吞吐（俊阳 ↔ 小青）”
- 表现为需求澄清、优先级裁决、完成定义与决策包质量不足

v1 运行规则：
- 每个周期只认 1 个主制约
- 总 WIP <= 3
- 所有任务先补齐验收标准再进实现
- 每日启动 / 每日收尾 / 每周复盘按模板运行

教训：
- 如果没有先把问题收敛成系统目标、当前制约和决策点，就容易过早陷入执行细节
- 子进程应该承担具体任务，小青负责系统调度、制约识别和节奏控制

---

## 2026-04-08 17:05 (Asia/Shanghai)

### 建立任务系统与任务回复模板

背景：
用户指出多任务并行时容易失去焦点，要求建立一套任务系统，并优化主进程与子进程的回复模板。

用户要求：
1. 每次任务相关回复都带上当前进行的是哪项任务
2. 如果当前任务完成，则回复该进程其他进行中和未完成的任务
3. 普通聊天不算任务

已完成：
- 创建 `TASKS.md` 作为任务单一事实来源
- 创建 `docs/TASK-SYSTEM.md` 作为任务系统设计说明
- 更新 `docs/TOC-TEMPLATES.md`，加入任务相关回复模板
- 更新 `docs/spawn-task-templates.md`，要求子进程任务回复包含当前任务与其他未完成任务
- 更新主协议与各子进程协议，将任务跟踪入口从 `TASK-PROGRESS.md` 切换到 `TASKS.md`
- 将根目录 `TASK-PROGRESS.md` 降级为兼容入口

关键决策：
- 普通聊天明确不进入任务系统
- `TASKS.md` 维护“当前聚焦 / 进行中 / 未完成 / 最近完成 / 按进程视图”
- 默认只允许 1 个当前聚焦任务，以抑制不良多工
- 当前任务完成时必须暴露本进程其他未完成任务，避免做完即忘

当前聚焦任务：
- `TASKSYS-001` 设计任务系统与回复模板

后续：
- 在后续任务中实际使用新回复模板
- 观察该任务系统是否真实降低失焦和上下文漂移

---

新增架构设计：优化待办任务查看流程（T-20260408-003）

已完成：
- 更新 `AGENTS.md`，把 `TASKS.md` 明确为启动时的待办内存快照入口
- 新增启动恢复后的“待办恢复卡片”规则，要求 gateway 重启或中断恢复后优先显示简洁待办
- 更新 `docs/TASK-SYSTEM.md`，加入 `TodoContext` 最小结构、高频待办查看路径、待办卡片模板、伪代码函数设计
- 更新 `BOOT.md`，补充恢复完成后直接基于 `TASKS.md` 显示待办的设计

关键决策：
- 高频待办查看默认走 `TASKS.md -> TodoContext -> 回复`，不再把扫描 `tasks/*.yaml` 作为默认路径
- 只有在摘要缺失、过期、冲突，或用户明确要求细节时，才回退到 TaskCard YAML
- 启动恢复提示与待办速览合并输出，降低 token 和操作成本

预期效果：
- 待办查看从“多文件扫描”收敛为“单文件启动加载 + 会话内存复用”
- 与现有 TOC 的单一事实来源 `TASKS.md` 保持一致，不引入第二套任务真相源

---

新增规则：统一回复进度表模板

已完成：
- 将“项目进度表 + 当前进行中任务状态 + 正文”的默认回复结构写入 `AGENTS.md`
- 将同一套规则写入 `memory/long-term/BEHAVIOR-PROTOCOL.md`
- 将展示模板、排序规则、颜色计算规则写入 `docs/TASK-SYSTEM.md`

确认的展示规则：
- 表格列固定为：排序、项目/任务、优先级、起始时间、结束时间、时间进度、状态、当前进展、下一步
- `项目/任务` 使用文字标题
- `状态` 只显示色块，不加文字
- 没有 `deadline` 的任务显示 `结束时间=-`、`时间进度=-`、`状态=-`，并统一排在最后

确认的逻辑规则：
- 排序：先按结束时间，再按优先级，再按起始时间；无 deadline 排最后
- 颜色：时间进度 `<1/3` 为 `🟩`，`<2/3` 为 `🟨`，`<1` 为 `🟥`，`>=1` 为 `⬛`
- 如果没有活跃任务，当前任务状态表格显示一整行 `无 / - / - / - / - / - / -`

后续调整：
- 将“当前进行中任务状态”从列表改为表格展示
- 新增字段：`执行agent`
- 当前任务状态表固定列为：当前任务、执行agent、状态、当前动作、当前进展、下一步、风险/阻塞

---

## 2026-04-08 21:33 (Asia/Shanghai)

### BOOT-OPT-001 架构设计完成

背景：
用户要求优化 Gateway 启动时的中断任务恢复逻辑，并将启动回复收敛为“两块式模板”。

已完成：
- 产出架构设计文档：`docs/arch-BOOT-OPT-001-boot-recovery-and-reply-template.md`
- 明确 boot 恢复只处理 canonical 状态机中的 `Released` / `Executing` 且未完成任务
- 明确 `Ready` 不自动恢复，只保留在待分配池
- 明确恢复顺序必须为：TaskCard 扫描 → 恢复执行 → 重建 `TASKS.md` → 渲染启动回复
- 设计两块式启动回复模板：`中断任务结果` + `任务回复模板内容`

关键决策：
- `tasks/*.yaml` 是恢复权威源，`runs.json` 只做运行态辅助证据
- 保留现有 `recover-interrupted.sh` 作为入口，但将业务判断下沉到统一任务层
- boot 恢复与 LOGIC-001 的 `release_task()` / 状态机约束保持同一套模型，不新增第二套恢复状态

实施建议：
- 总工期建议 1.5 到 2 天
- 先接 canonical 状态机，再重构恢复链路，最后接模板与测试

---

## 2026-04-08 21:48 (Asia/Shanghai)

### Boot恢复回复模板优化完成

背景：
用户要求进一步优化boot恢复回复模板，使其更加简洁实用。

优化要求：
1. 任务状态表格只展示未完成的任务
2. 进度栏只保留时间进度色块（🟩🟨🟥⬛ + "-"）
3. 中断任务结果改为"任务恢复说明"
4. 任务使用title，不用任务编号
5. 删除说明文字，说明文字保留在文档中

已完成：
- 创建最终优化模板：`templates/boot-recovery-reply-template.md`
- 更新恢复脚本：`~/.openclaw/scripts/recover-interrupted.sh`
- 创建示例文件：`examples/boot-recovery-example.md`
- 更新AVATAR-001任务状态为Done（用户手动修复完成）

最终模板结构：
```
## 📋 Boot恢复回复

### 任务恢复说明
- 恢复状态: <已恢复 N 个任务 / 未发现需恢复任务 / 发现 N 个任务但未自动恢复>
- 恢复任务: <任务title列表；无则写 无>
- 未自动恢复: <任务title + 原因；无则写 无>

### 任务状态表格
| 任务Title | 状态 | 进度 | 执行Agent |
|-----------|------|------|-----------|
| [任务标题] | [状态] | [进度色块] | [执行者] |
```

关键决策：
- 进度色块只表示时间进度，不表示工作量或其他因素
- 任务使用易理解的title，避免使用任务编号
- 所有说明文字移至文档中，回复保持极简
- 表格只展示未完成（非Done状态）的任务

实施效果：
- boot恢复回复更加简洁直观
- 用户能快速了解恢复状态和当前任务情况
- 符合"有则执行恢复，没有就算了"的设计原则

---

## 2026-04-08 22:32 (Asia/Shanghai)

### 任务分配逻辑问题识别与修复

背景：
用户指出任务分配逻辑有问题，要求让architect检查逻辑。

问题识别：
1. 状态模型冲突：TASKS.md/TASK-SYSTEM.md/TOC TaskCard使用两套状态语言
2. 真相源分裂：TASKS.md与tasks/*.yaml数据漂移
3. 分配逻辑只有模板，没有决策引擎
4. owner字段语义重叠，责任转移不可追踪

已完成：
- LOGIC-001任务完成架构审查
- 产出架构文档：`docs/arch-LOGIC-001-task-routing-review.md`
- 明确统一状态机：使用TOC canonical状态（Captured → Clarified → Ready → Released → Executing → Verifying → Done）
- 明确权威数据源：以`tasks/*.yaml`为唯一权威源，`TASKS.md`改为自动生成的摘要视图
- 设计显式路由引擎：新增`classify_request()`、`route_task()`、`release_task()`决策函数

关键建议：
- 先修状态机和真相源，不先修展示模板
- 先做`task-normalizer + TASKS.md生成器 + route_task()`
- 然后再推进BOOT-OPT-001，把恢复逻辑接到同一状态机上

实施时间表：
- Phase 0 止血：0.5天
- Phase 1 状态/字段收敛：0.5到1天
- Phase 2 分配逻辑产品化：1天
- Phase 3 视图与boot接线：1天
- Phase 4 回归验证：0.5天
- 总计：约2.5到3.5天

教训：
- 小青作为主助理的角色是任务分发和协调，而不是直接执行所有任务
- UI修复类任务应分配给专门的coder角色
- 决策包SOP帮助明确了目标、制约和最佳执行路径

---

## 2026-04-08 23:02 (Asia/Shanghai)

### 系统架构演进总结

今日关键进展：
1. **TOC协作系统建立**：完成系统蓝图和运行文档，确立围绕主制约的协作模式
2. **任务系统完善**：建立TASKS.md和任务回复模板，解决多任务失焦问题
3. **boot恢复机制设计**：完成中断任务恢复逻辑和标准化回复模板
4. **任务分配逻辑审查**：识别系统性问题，提出统一状态机和路由引擎方案
5. **模板优化完成**：boot恢复回复模板优化为简洁的两块信息结构

系统现状：
- **当前主制约**：主链路决策吞吐（俊阳 ↔ 小青）
- **系统健康度**：7.5/10（根据toc-monitor日终复盘报告）
- **关键风险**：验证环节可能成为新制约
- **明日重点**：解决阻塞任务，释放Ready队列，推进AVATAR-001修复

技术债务识别：
1. 状态模型需要统一（TOC canonical状态 vs 现有状态）
2. 数据源需要收敛（tasks/*.yaml为权威源，TASKS.md为派生视图）
3. 分配逻辑需要产品化（缺少显式路由决策引擎）
4. boot恢复需要集成（基于统一状态机实现恢复逻辑）

下一步优先级：
1. 🟢 高优先级：统一状态模型，收敛数据源
2. 🟡 中优先级：实现路由决策引擎
3. 🔴 低优先级：集成boot恢复，优化用户界面

今日最大收获：
- 成功验证了决策包SOP在真实任务中的应用
- 明确了小青作为协调者而非执行者的角色定位
- 建立了系统化的架构审查和改进流程
- 用户参与度高，反馈及时，推动了系统快速迭代

