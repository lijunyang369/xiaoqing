# 2026-03-19

- 用户再次明确纠正：涉及 OpenClaw 审批/权限问题时，应优先走官方路径（如 `openclaw approvals --gateway`）核对真实生效面，不应先靠手改猜测性文件。
- 复盘结论：后续排这种问题，顺序固定为：官方 CLI/官方配置面 -> 日志验证 -> 最小改动 -> 最后才做手工文件兜底。
- 用户新增日常交互要求：每天与用户的第一次交互时，主动展示 ngrok 状态，并明确告知域名是否发生变化。
- 已创建独立资讯智能体 `news-brief`（人格名：青闻），负责世界 AI / 世界科学 / 中国重要新闻的收集、交叉核验与每日简报候选。
- 每日第一次汇报已接入设计：默认由小青汇总，向青闻索取资讯候选，结构固定为 ngrok 状态 + 域名变化 + AI / 科学 / 中国新闻，并要求真实、拒绝杜撰、优先官方来源、多源核验、标注来源。
- 已创建独立浏览器智能体 `browser-ops`（人格名：青览），负责浏览器页面操作、Web 配置向导、GitHub 页面类任务。
- 用户新增全局执行要求：每次进入新任务前，若上下文已接近或达到上限，必须先做极短摘要并切换压缩续跑模式；不要再把“开新会话/进入下一个任务”的负担重复甩给用户。
- 用户纠正 TOC 认知：最核心应抓“事物的固有简单性”；解决问题要从根因分析，而不是先把问题复杂化。
- 用户新增状态汇报触发条件：不是每次 start，而是每次 gateway restart 之后，应主动汇报当前状态与变更结果。
- 任务状态现统一维护在 `workspace/TASKS.md`，后续需持续更新，方便随时查看当前进度、阻塞点与下一步。
- GitHub/浏览器任务当前关键根因：OpenClaw 官方 browser 能力存在，`openclaw` profile 可识别且已成功拉起 Chrome；浏览器服务端口 18791、chrome-relay 18792、CDP 18800 均可监听。当前阻塞点已收敛为“启动后接管/控制链不稳”，不是“Chrome 未启动”。
- Auto-compaction 任务当前归类为“待验证”：官方支持且配置已从 `safeguard` 切为 `default`，但尚未有可靠证据证明在长会话中已正常无感生效；后续再次触发上下文逼近上限时应优先复查该点。
