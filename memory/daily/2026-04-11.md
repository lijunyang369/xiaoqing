## 16:10 - 优化 debugger 工作协议
- 将 debugger 从“测试者 + 调试者混合角色”收敛为“排障专家”
- 明确与 tester 的边界：常规验证、回归、提交流水线测试归 tester；异常定位、复杂复现、根因判断归 debugger
- 将 debugger 默认流程统一为：现象定义 → 分层验证 → 证据收敛 → 结论与移交建议
- 删除笨重默认产出要求，不再默认要求 `test-report.md`，临时排查记录默认进入 `temp/`
- 修复标准读取入口缺失问题，新增 `protocols/BEHAVIOR-PROTOCOL-DEBUGGER.md` 作为入口索引

## 16:40 - 固化 boot 回复口径
- 用户明确要求：boot **无论什么结果都要按模板回复**
- 更新 `BOOT.md`：禁止因为“nothing needs attention”而回复 `NO_REPLY`
- 明确以 `BOOT.md` 为 boot 场景下的最终判定口径

## 16:50 - 收敛子进程协议读取链路
- 将 `behavior-protocol-enforcer` 技能的协议路径从 `memory/long-term/` 收敛到 `protocols/` 与 `protocols/roles/`
- 更新 `session-detector.js`、`read-protocol.js`、`package.json`、`SKILL.md`
- 更新 `config/agent-role-registry.json` 的维护说明，避免继续引用旧路径和失效 spawn 模板文档
- 冒烟验证通过：新起 `coder` 子进程可稳定读取 `protocols/roles/BEHAVIOR-PROTOCOL-CODER.md`

## 16:55 - 删除遗留 behavior-protocol-enforcer skill
- 用户确认 `behavior-protocol-enforcer` 属于历史遗留，不再需要
- 已移除 `~/.openclaw/skills/behavior-protocol-enforcer`
- 后续子进程协议读取应以 `protocols/`、`protocols/roles/` 与 spawn task 明示路径为准

## 16:56 - 固化 spawn 协议读取最小方案
- 为 architect / coder / env-engineer / debugger / tester 补齐统一的“协议读取规则”段落
- 新增 `scripts/build_spawn_task.py`，从 `config/agent-role-registry.json` 读取角色协议路径，生成标准 spawn task 文本
- 更新主协议的 Spawn 要求：优先通过 `python3 scripts/build_spawn_task.py <role> "<任务正文>"` 构造 task
- 二次冒烟验证通过：使用标准生成器产出的 task 文本起 `coder` 子进程，成功读取 `protocols/roles/BEHAVIOR-PROTOCOL-CODER.md`

## 16:57 - 修复新任务开始时进行中任务为空
- 定位根因：开始“子进程协议优化与 spawn 协议读取稳定化”任务时，未同步创建 active 任务卡片，导致 `TASKS.md` 进行中任务仍显示为空
- 新建 `tasks/active/T-20260411-001.md`
- 更新 `TASKS.md`，将该任务挂入“进行中任务”

## 17:06 - 固化新任务先建卡规则
- 新增 `scripts/create_task.py`，用于创建 active task card 并同步写入 `TASKS.md`
- 更新主协议第 4.4 节：新任务若无可挂接 active task，必须先建 task 再继续执行/分发/汇报
- 明确禁止：当前任务尚未挂接时，直接默认并行 spawn 多个子进程
- 轻量验证通过：`create_task.py` 语法正常，dry-run 可生成新任务 ID

## 17:10 - 正式改正协议层级
- 明确层级：公共协议给所有进程遵守；主协议仅给小青主进程参考与遵守；子协议仅给各自角色遵守
- 更新主协议第 5.2 节，修正“主协议被所有子进程共同参考”的错误表述
- 更新各角色子协议的“协议读取规则”，统一改为“先遵守公共协议，再阅读本角色子协议”
- 更新 `config/agent-role-registry.json` 与 `scripts/build_spawn_task.py`，使 spawn task 文本体现新的读取顺序

## 17:15 - 删除协议镜像
- 按用户要求，删除 `memory/long-term/` 下的角色协议镜像文件
- 保留 `protocols/` 作为协议权威源
- `memory/long-term/` 仅保留公共协议 `BEHAVIOR-PROTOCOL-COMMON.md`

## 17:23 - 删除子协议中的激活条件
- 按用户选择的方案 B，删除 plan / architect / coder / env-engineer / debugger / tester 子协议中的“激活条件”段落
- 子协议收敛为纯执行协议：只定义角色拿到任务后如何工作，不再假装参与调度
- 顺手统一章节编号，避免删除段落后出现编号断层

## 17:27 - 收紧子进程协作规则
- 将各子协议的协作规则统一改为：子进程只对自己的产出负责，并统一汇报给小青，由小青调度后续协作
- 删除“子进程彼此直接协作/直接通知/直接移交”的误导性表述
- debugger 协议同步修正协作规则编号与异常处理小节编号

## 17:31 - 删除 long-term 公共协议镜像
- 按用户要求，删除 `memory/long-term/BEHAVIOR-PROTOCOL-COMMON.md`
- 仅保留 `protocols/BEHAVIOR-PROTOCOL-COMMON.md` 作为公共协议权威源
