# 2026-04-07

- 用户新增稳定工作流偏好：参考 Cursor，把对话/执行区分为 Ask、Plan、Debug 三类。
- 用户明确认可 Plan 模式：先规划好，再生成 todo 跟踪，用来指导 agent 不发散。
- 已将 Ask / Plan / Debug 固化为默认工作协议，并补充默认切换规则：提问走 Ask，长任务走 Plan，异常排查走 Debug；若规划与排障混合，先 Debug 收敛边界，再切 Plan。
- 用户新增规则：做 Plan 时默认启用高推理模式，因为计划做得好，后续执行会事半功倍。
- 用户要求将 DeepSeek 模型与 OpenAI 模型都配置为可用，已更新配置支持双模型切换。
- 用户进一步要求加入 DeepSeek Reasoner 和其他 GPT 模型，已添加 DeepSeek-Reasoner、GPT-4-Turbo、GPT-4o、GPT-4o-mini 到模型列表。
- 用户询问记忆系统分层，已解释当前 4 层结构：长期记忆（MEMORY.md）、会话上下文、线程上下文、self-improving 学习层。
- 用户提议挂载 GraphRAG 作为知识库增强层，讨论后确认 GraphRAG 应作为“检索与推理增强层”接入，不替代现有记忆层。
- 用户认可 Cursor 的 Ask/Plan/Debug 模式，已固化为默认工作协议，并补充切换规则。
- 用户要求 Plan 模式默认启用高推理模式，已记入默认规则。
- 用户决定采用方案 B（新开子进程）执行 GraphRAG 建设，专职 agent“青思”已启动并产出详细实施计划。
- 青思的 GraphRAG MVP 计划包含 5 个步骤：Schema 定义、数据提取、图谱构建、检索策略、验证测试，预计 3-5 天完成。
- 需要用户确认的关键决策：图数据库选型（建议 Neo4j Docker）、数据提取策略（先用规则解析）、检索结果展示格式、集成方式。
- 下午深入讨论 GraphRAG 技术选型：用户希望用 DeepSeek embedding，但验证发现 DeepSeek 未提供公开 embedding API。
- 用户确认最终方案：放弃微软 GraphRAG，改用 LlamaIndex（Knowledge Graph Index），embedding 用开源中文模型 `BAAI/bge-small-zh-v1.5`（本地运行），LLM 用 DeepSeek（主）+ OpenAI（备用）。
- 用户硬件配置：GTX1050（2-4GB 显存），尝试 GPU 加载，不足时回退 CPU。
- 青思已启动实施新方案，预计 3 天完成 MVP。
- 模型配置更新：默认模型改为 DeepSeek-Chat，删除 Gemini 模型配置，备选模型保留 DeepSeek-Reasoner、OpenAI-GPT-5.4、GPT-4o、GPT-4o-mini、GPT-4-Turbo。
