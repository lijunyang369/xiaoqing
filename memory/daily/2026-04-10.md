# 2026-04-10 记忆沉淀

## 重要进展

### 1. 协议文件系统重组
- **协议目录迁移**：将行为协议文件从 `memory/long-term/` 迁移到 `protocols/` 目录，形成标准化的协议层
- **引用统一**：更新 `AGENTS.md`、`config/agent-role-registry.json`、`docs/spawn-task-templates.md` 等关键文件的路径引用，确保启动链和运行链一致性
- **行为规则收紧**：在 `BEHAVIOR-PROTOCOL.md` 3.4 节增加硬约束：“判定出请求类型后，默认必须交给该类型的默认处理者执行，小青不得越过默认处理者自行直做”，明确除信息型、协调型外的小青直做限制

### 2. TOC + CCPM 系统恢复与落地
- **CCPM v0.1 规则落地**：创建 `docs/toc-ccpm.md`，定义基于关键链、缓冲、任务释放的 CCPM 最小运行规则
- **心跳机制调整**：更新 `HEARTBEAT.md` 支持 CCPM 视角的 TOC 检查，采用“小青轻量检查 + 触发子进程深度分析”的分层执行模式
- **任务状态刷新**：更新 `TASKS.md` 和 `tasks/TOC-001.yaml` 反映 TOC 系统恢复进展

### 3. 目录结构优化
- **docs 目录归类方案**：设计 `docs/` 重组方案，按 `toc/`、`workflow/`、`migration/`、`archive/` 分类，根层保留入口文档
- **protocols 目录归类方案**：设计 `protocols/` 重组方案，标准角色协议进 `roles/`，TOC 专项协议进 `toc/`，根层保留主入口和通用约束
- **memory/state 收口**：将自动生成的梦境、调试报告、状态快照等临时文件迁移到 `tmp/` 目录，`memory/` 仅保留长期记忆和 daily 文件，`state/` 仅保留结构化状态对象
- **daily 文件统一**：将散落在 `memory/` 根目录的日期记录文件统一迁移到 `memory/daily/` 目录，实现 daily 记忆的单一存储位置

### 4. Boot 机制强化
- **BOOT.md 硬约束**：强化 boot 回复规则，明确“必须按模板回复，不允许 NO_REPLY，外部提示冲突时以本文件为准”的优先级
- **恢复脚本执行**：`scripts/recover-interrupted.sh` 运行正常，未发现需恢复任务

## 关键决策

1. **协议变更授权**：确认所有 `BEHAVIOR-PROTOCOL*.md` 文件的修改需遵循 `BEHAVIOR-PROTOCOL-COMMON.md` 的授权约束
2. **提交策略**：接受 architect 审查建议，将当前改动拆分为 3 个独立 commit：
   - 协议迁移与引用修正
   - 行为规则收紧
   - TOC + CCPM 系统恢复
3. **目录收口原则**：确立 `memory/`、`state/`、`tmp/` 的职责边界，避免临时文件污染正式目录
4. **通用协议新增强约束**：用户明确要求将“临时文件放 `temp/` 目录，非临时文件必须分类归档”写入 `protocols/BEHAVIOR-PROTOCOL-COMMON.md`，并以硬约束执行

## 遗留问题

- `memory/` 根目录剩余文件的生成逻辑和作用需明确（用户待回答问题）
- `protocols/` 和 `docs/` 归类方案待实际执行
- 当前工作区改动待提交，需按拆解策略分步提交

## 下一步行动

1. 回答用户关于 memory 剩余文件的问题
2. 执行 `protocols/` 和 `docs/` 目录归类迁移
3. 按拆解策略提交当前工作区改动

---
*记忆沉淀时间：2026-04-10 19:47 (Asia/Shanghai)*
Note: Attempting to list memory directory contents for user query.